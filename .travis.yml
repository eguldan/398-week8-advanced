language: python
python:
  - "3.5"
install:
    # - pip install python-coveralls
  - pip install coverage
    # - pip install nose
script:
  - make test
  - coverage run -m unittest
    # - nosetests --with-coverage
after_success:
  - coveralls
